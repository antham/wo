name: Go

on:
  push:
    tags:
      - v*
    branches:
      - master
      - main
  pull_request:
permissions:
  contents: read

env:
  TAG: e2e

jobs:
  e2e:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: Build the image
        uses: docker/build-push-action@v5
        with:
          context: .
          load: true
          file: ./Dockerfile.test
          tags: ${{ env.TAG }}
      - name: Bash tests
        run: |
          docker run --rm ${{ env.TAG }} bash e2e-scripts/test.bash
